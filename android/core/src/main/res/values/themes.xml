<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.We" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/we_ivory</item>
         <item name="colorPrimary">@color/we_ivory</item>
        <item name="materialSwitchStyle">@style/CustomSwitch</item>

    </style>

    <style name="Theme.We" parent="Base.Theme.We" />
</resources>